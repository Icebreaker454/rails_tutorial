<% if notice %>
	<p id="notice"><%= notice %></p>
<% end %>
<h2>Your Pragmatic Cart</h2>
<ul>
	<% @cart.list_items.each do |item| %>
		<li>
			<%= item.quanity %> &times; <%= item.product.title %>
		</li>
	<% end %>
</ul>